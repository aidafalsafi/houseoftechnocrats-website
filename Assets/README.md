# Assets

This folder contains the media files used by the website pages.

- Root files: site images and podcast audio files referenced from `site.js` and `index.html`.
- `fonts/iranyekanx`: web fonts used by `site.css` and the IA diagram pages.
- `Fonts`: TTF fonts used by `technocratic-thinking-toolkit.html`.
